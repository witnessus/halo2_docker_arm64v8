Halo2 blog docker-compose file, using postgres, running on ARM devices, tested on Thunder's OEC Turbo.

1. Clone this repo, cd to this project's dir, then use `docker-compose up -d` to run this docker.
2. I use the agent to pull docker images, because I cannot access docker registry directly.
